apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: orders-api
  namespace: monitoring
  labels:
    release: kps
spec:
  namespaceSelector:
    matchNames:
    - cloudshop-prod
  selector:
    matchLabels:
      app: orders-api
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
